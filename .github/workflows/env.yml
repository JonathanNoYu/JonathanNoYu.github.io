name: Secrets and Enviroment Variables

on:
  workflow_run:
    workflows: [pages-build-and-deployment, Node.js CI]
    
jobs:
  setup-env:
    runs-on: ubuntu-latest
    env:
      API_URL: ${{ secrets.API_URL }}
