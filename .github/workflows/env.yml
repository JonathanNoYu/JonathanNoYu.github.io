name: Secrets and Enviroment Variables

on:
  push: 
    branches: [prod, gh-pages]
    
jobs:
  setup-env:
    runs-on: ubuntu-latest
    env:
      API_URL: ${{ secrets.API_URL }}

    steps:
      - name: Read secret var
        run: echo "#{{ secrets.API_URL}}"
